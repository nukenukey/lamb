import(lamb:standard);
import(lamb:math);
start(unknown):4
	0
;
-comments!
next(start):4
	sub (start)(2)
- returns -2
	
;
after(next):4
	dec(start)
- returns -3
;
last(after):void
	:print(after)
- does not return
;
